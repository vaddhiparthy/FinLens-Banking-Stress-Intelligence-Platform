Coverage summary (pytest-cov), generated from `pytest tests/ ml/tests/ --cov`.
Full per-line report: audit/tech/test_results.txt.

TOTAL: 52% (2285 statements, 1096 missed). 82 tests passed, 0 failed.

Well-covered (the served decision path):
  ml/finlens_ml/scenario.py    92%   (live scoring used by the UI)
  ml/finlens_ml/serve.py       90%   (FastAPI serving, incl /predict-failure-risk)
  ml/finlens_ml/splits.py      91%   (the embargoed out-of-time split)
  ml/finlens_ml/evaluate.py    high  (metrics + bootstrap CIs)
  ml/finlens_ml/features.py / failure_cause_labels.py  covered by tests
  src/finlens: config 96%, telemetry 96%, pipeline_status/state/paths/logging 100%

Low coverage, and why (offline pipeline / infra code, exercised by running it, not unit tests):
  ml/finlens_ml/train.py        11%  (offline training pipeline; its OUTPUT, the served
                                      artifact + metrics, is what is validated, plus the
                                      decomposition/eval tests; re-running it is the check)
  src/finlens/warehouse.py      33%, platform_probes.py 19%, datasets.py 53%  (DuckDB/
                                      warehouse plumbing run by the Airflow DAGs)
  src/finlens/evidence.py 0%, pipeline_runs.py 0%  (control-plane logging, run at runtime)

Interpretation: the code that produces every number the UI shows (scoring, serving, splits,
evaluation) is well covered; the uncovered code is offline orchestration/warehouse plumbing
whose correctness is verified by actually running the pipeline (dbt build SUCCESS, GX 20/20,
the 82 tests) rather than by unit tests. Raising the headline % would mean unit-testing
orchestration glue, which is low value versus the end-to-end run that already passes.
