"""FullLens Analyst Assistant (Capstone 3): local, $0 RAG over the regulator-sourced
failure-cause corpus + the model's own methodology findings. Chroma (local persistent) +
sentence-transformers embeddings; LangGraph orchestration; local Ollama for synthesis."""
